# Payload
# openssl rand -hex 32
PAYLOAD_SECRET=
# local: postgresql://<user>@127.0.0.1:5432/mea
DATABASE_URI=
# 1 to log SQL queries from Payload/Postgres for query-budget checks
PAYLOAD_LOG_SQL=0

# Next
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
